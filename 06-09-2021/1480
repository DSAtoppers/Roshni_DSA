class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        vector<int>run;
        // run.push_back(nums[0]);
        int sum=0;
        for(int i=0;i<nums.size();i++)
        {
            sum+=nums[i];
            run.push_back(sum);
        }
        return run;
    }
};
